import { IAdvisory, Totals } from "../interfaces";
export declare const renderAdvisories: (advs: IAdvisory[]) => string;
export declare const renderTotals: (totals: Totals, totalCount: number) => string;
